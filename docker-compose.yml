# Local development only — production deploys via Vercel (no Dokploy/Traefik).
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: production

    expose:
      - "3000" # internal only; docker-compose.override.yml maps it to localhost for dev
